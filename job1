pipeline {
     agent any
     stages {

     stage ('Build JobName')
     {
        steps {
                
                echo "Job Name is ${currentBuild.name}"
               }
      }
     stage ('Build No.')
     {
        steps {
                
                echo "Build number is ${currentBuild.number}"
               }
      }
  }
}
