pipeline {
     agent any
     stages {

     stage ('Build No.')
     {
        steps {
                def buildNumber = currentBuild.number
                echo "Build number is ${currentBuild.number}"
               }
      }
  }
}
