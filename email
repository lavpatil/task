pipeline {
    agent any
     
    stages {
        stage('Ok') {
            steps {
                echo "Ok"
            }
        }
    }
    post {
        always {
            mail to:'lavpatil2015@gmail.com',
            subject:"Hi This Is First Jenkins",
            body:"Job Name is ${JOB_NAME}",
            body:"Build number is ${currentBuild.number}",
            body:"Job URL is ${JOB_URL}"
            
        }
    }
}
